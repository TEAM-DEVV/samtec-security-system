{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm exec prisma generate && { [ \"$VERCEL_ENV\" = production ] && pnpm exec prisma migrate deploy || echo 'Not the production deployment: migrations left alone.'; } && pnpm build",
  "framework": null,
  "outputDirectory": ".",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "crons": [{ "path": "/api/v1/detection/daily-sweep", "schedule": "0 2 * * *" }],
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "includeFiles": "dist/**"
    }
  }
}
